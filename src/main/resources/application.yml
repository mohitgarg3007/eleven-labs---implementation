spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/textaudio
    username: postgres
    password: xxxxx
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
